name = "HCProof"
version = "0.1.0"
defaultTargets = ["HCProof"]

[[require]]
name = "mathlib"
scope = "leanprover-community"
rev = "v4.33.0-rc2"

# ---------------------------------------------------------------------------
# THE HC UNIVERSE — one library, one root closure.
#
# `Main` transitively imports the entire universe: CardinalWorlds (Layer 1),
# MonolithBoundary (Layer 5), the GST Graph V2 Ontological Universe
# (Layers 2-4), and HodgeDeRhamBridge (Layer 6, the absorption).
# The comparator smoke pair rides alongside.
# ---------------------------------------------------------------------------
[[lean_lib]]
name = "HCProof"
roots = [
  "Main",
  "ComparatorSmokeChallenge",
  "ComparatorSmokeSolution",
]
